import { AppBar, Toolbar, Typography } from "@mui/material"

const Navbar = () => {
    return (
        <AppBar>
            <Toolbar>
                <Typography variant="h6">
                    MUI-APP
                </Typography>
            </Toolbar>
        </AppBar>
    )
}

export default Navbar